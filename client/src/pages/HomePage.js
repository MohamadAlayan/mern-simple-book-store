import React from 'react';
import {Typography} from "@mui/material";

const HomePage = () => {
    return (
        <div>
            <Typography variant={"h1"} style={{ textAlign:"center",fontWeight:"bolder"}}> Home Page</Typography>
        </div>
    );
};

export default HomePage;